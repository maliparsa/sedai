TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=
# Comma-separated Telegram user IDs allowed to use the bot, e.g. 111111111,222222222
# ORDERED list: the FIRST ID is the admin (can manage users, API keys, and default models).
# Other IDs are regular users (can only set their own per-user model preferences).
ALLOWED_USER_ID=
